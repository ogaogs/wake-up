/// MVP-fixed tuning values. (Made configurable in a later milestone.)
public enum WakeupConfig {
    /// You must get this far from home for the alarm to stop.
    public static let geofenceRadiusMeters: Double = 300
}
